version: '3.8'

services:
  cryptorsi:
    build: .
    ports:
      - "3000:3000"
    environment:
      - DATA_DIR=/app/data
      - PORT=3000
    volumes:
      - cryptorsi-data:/app/data
    restart: unless-stopped

volumes:
  cryptorsi-data:
